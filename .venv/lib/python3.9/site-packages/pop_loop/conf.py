CONFIG = {
    "backend": {
        "type": str,
        "default": "auto",
        "help": "Select the loop plugin to use as the asyncio backend. Defaults to 'auto'",
    }
}

DYNE = {"loop": ["loop"]}
